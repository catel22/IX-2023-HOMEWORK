// Constructor takes arguments we want to define our task

export class Book {
    constructor(id, title, author, isbn, userId) {
        this.id = id;
        this.title = title;
        this.author = author;
        this.isbn = isbn;
        this.userId = userId;
    }
}